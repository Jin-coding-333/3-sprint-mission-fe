/**
 * Displays a loading indicator with the message "로딩 중입니다..." in Korean.
 *
 * @returns A React element showing a loading message.
 */
export default function Loading() {
  return <div>로딩 중입니다...</div>;
}
